{
  "name": "${NAME}",
  "version": "${VERSION}",
  "description": "${DESCRIPTION}",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/${IDENTIFIER}/${NAME}.git"
  },
  "keywords": [
    "6502",
    "nes",
    "assembly"
  ],
  "author": "${MAINTAINER} <${EMAIL}>",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/${IDENTIFIER}/${NAME}/issues"
  },
  "homepage": "https://github.com/${IDENTIFIER}/${NAME}#readme"
}
